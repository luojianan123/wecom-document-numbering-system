# 文件编号系统 · 用户使用教程

> 本文面向**普通用户**（非管理员），说明如何通过「用户界面」完成两种编号业务：
> **① 文件编号**（查询 / 领取 / 申请新编号）和 **② 产品组成编号**（产品层级编制与编码）。
> 界面文案、流程、提示均以当前系统为准。

---

## 一、进入系统与登录

浏览器打开系统地址（按实际部署地址，本地开发通常为 `http://localhost:5173`）。

**登录方式（取决于部署配置）**

- **本机 / 开发环境**（mock 登录）：页面上直接提供「用户界面」与「管理员界面」入口。普通用户点「用户界面」即可进入。
- **生产环境**（企业微信扫码）：用企业微信扫一扫登录。扫码后在授权页确认即可。

登录后，普通用户默认进入 **「选择编号类型」** 页，有两个业务入口：

| 编号 | 卡片 | 说明 |
|---|---|---|
| 01 | 产品组件编号 | 产品、板卡、软件及其他组件编号。 |
| 02 | 文件编号 | 查询文件编号、申请新编号，并领取和复制编号。 |

> 若系统用微信身份，登录人身份由后台配置；普通用户看不到「管理员界面」。

---

## 二、文件编号（查询 / 领取 / 申请新编号）

> 对应页面：`/user/files`，标题「**编码领取**」。
> 整体流程：**找到文件 → 领取正确编码**。

### 2.1 选择项目

在「项目」输入框中搜索并选择项目：

- **输入项目号或项目名**（例如 `2222` 或项目名）→ 下方弹出匹配项目列表 → **点击选中**。
  - 当你输入一个**完整的 4 位项目号**且正好命中已有项目时，系统会**自动选中**该项目，无需再点。
- 选中后显示：项目号、项目名、右上角「显示全部」按钮和「×（清除重选）」按钮。
- 若该项目被标记为**特殊编号项目**，会显示「特殊编号项目」标签，并有提示：*“该项目有其他编号要求。已有编号可正常领取；申请新编号时，请等待管理员人工编号。”*

**项目不在列表里？申请新项目编号**

- 在搜索框输入一个**尚未存在的 4 位项目号**，点击右侧的「**申请新项目编号**」。
- 系统会向管理员发送新项目申请，管理员建立项目后你即可使用。提示：*“已提交新项目编号申请，并通知管理员”*。

### 2.2 输入文件名称

选中项目后，用三个字段拼出文件名称（下方会实时显示“合并后文件名称”）：

| 字段 | 是否必填 | 说明 |
|---|---|---|
| **产品/部组件** | 必填 | 例如“主控板”“智能控制设备”。点击该框会**弹出**该项目已登记的产品/板卡/软件名称，**点选即可填入**；也可以**不选、直接手输**任意内容（匹配不到时提示“未找到匹配名称，可继续手工输入”）。 |
| **阶段** | 可不填 | 例如“鉴定件”“正样件”。用于确定阶段号与版本号（见下）。 |
| **文件类型** | 必填 | 例如“开发计划”“方案设计报告”“技术要求”。 |

- 三个字段会**自动拼成一个文件名**，用于查询 / 生成。
- 阶段与版本对应关系：
  - 含「正样件」→ 阶段 `Z`，版本 `2.00`
  - 含「鉴定件」→ 阶段 `C`，版本 `1.00`
  - 都不含 → 无阶段，版本 `1.00`

### 2.3 查询编码

点击「**查询编码**」：

- **查到了**：下方「查询结果」列出匹配文件，每条显示文件名、来源（项目初始化／缺失补码）、**完整文件编号**，以及分解信息（**级别 / 功能 / 简号 / 版本**）。点「**领取并复制**」即可领取并把编号复制到剪贴板。
- **没有完全匹配，但有相近文件**：顶部提示 *“当前结果为库中的相近文件。可以直接领取已有编号；若都不是所需文件，请提交…进行名称检测。”* 此时可领取已有编号，或点「**检测并生成新编号**」。
- **完全搜不到**：显示「未找到对应编码」，下方按钮为：
  - 普通项目 →「**检测名称并生成编号**」
  - 特殊编号项目 →「**提交管理员人工编号**」

> 右上角「**显示全部**」可一次列出该项目下所有可领取的文件与编号。

### 2.4 领取编号

点「**领取并复制**」后：系统记录**领取人**与**领取时间**，并把编号写入剪贴板，提示 *“编码已领取并复制”*。用于识别该文件由谁领取，避免重复领用。

### 2.5 申请新编号（补码）

在“未找到对应编码”时点「**检测并生成新编号**」，系统会校验名称并按规则编号。**按名称情况，结果分四类**：

| 情况 | 结果 | 提示 |
|---|---|---|
| 名称正常、且无相似文件 | **直接生成编号** | “文件名称校验通过，编号已生成” |
| 名称已存在 | 返回已有编号 | “文件名称已存在，已返回现有编号” |
| 检测到**相似文件名称** | 提交管理员审核 | “检测到相似文件名称，已提交管理员审核” |
| 名称**明显无关**（生活化/个人表达/非工程） | 提交管理员审核 | “文件名称疑似非工程内容，已提交管理员审核” |
| **特殊编号项目** | 直接提交人工编号 | “该项目有其他编号要求，请等待管理员人工编号” |

**名称会被自动拒绝的情况**：纯数字、连续重复字符、网址、表情、明显无意义的特殊符号、明显无关内容。此时会提示具体原因，或转交管理员处理。

> 审核通过后，管理员会把**正确名称对应的编号**返回给你；你可在「我的申请」里查看并领取。

### 2.6 我的申请（名称审核）

页面下方「我的申请」列出你提交的名称审核，每条显示：

- 名称（审核后名称 / 建议名称 / 原始名称）
- 相似名称列表（若有）
- 审核状态：**待管理员审核 / 审核通过 / 已驳回**
- 若已通过：显示生成的编号，并提供「**领取并复制**」。

---

## 三、产品组成编号（层级编制）

> 对应页面：`/user/components`，标题「**产品组件层级编制**」。
> 适合为“产品 → 部组件 → 结构/硬件 → 零件”这类层级编制并生成编码。

### 3.1 进入与项目

1. 在「选择编号类型」点「**产品组件编号**」。
2. 输入**4 位项目号**，点「**进入项目**」。
   - 若该项目已建立产品组成，会提示“该项目已有产品组成编号”，**类型已固定**。
   - 若尚未建立，则进入下一步选择类型。

### 3.2 选择产品组成类型

| 类型 | 说明 |
|---|---|
| **整机** | 保留整机层级，按现有编号形式编制。 |
| **结构件** | 去掉整机，从部组件开始编制结构。 |
| **单板卡** | 去掉整机，从部组件开始编制板卡。 |

点「**进入填写**」进入层级编辑器。

### 3.3 编制层级树

左侧是「产品组成」层级树，右侧是当前选中节点的编辑区。

- **增加根节点**：整机类型点「增加整机」；结构件/单板卡类型会自动生成一个**部组件**根节点。
- **逐级增加下级**：
  - 整机 → 可加「部组件」
  - 部组件 → 可加「结构 / 硬件 / 软件 / 其他」
  - 结构 / 硬件 → 可加「零件」
  - （软件/逻辑：页面提示“为保证与项目文件编号规则一致，请转到文件编号处对软件/逻辑的代码进行编号”。）
- **填写名称与阶段**：选中一个节点，右侧输入“名称”，并选择「研制阶段」：
  - `C` 初样件/电性件
  - `M` 模样件
  - `Z` 正样件
  - `G` 其他
- **自动子项**：某些父级名称会自动生成常用下级，例如：
  - “机箱 / VPX机箱” 结构件 → 自动给出“前面板、后面板、上盖板、下盖板、左右侧板…”等建议；
  - 某“板PCBA” 硬件 → 自动给出“该板PCB、该板PCB设计”等建议。
  自动生成的项可直接修改或删除。
- **删除**：每个节点有「删除」按钮；删除某个节点会**连同其全部下级**一起删除，删除后需重新“统一生成全部编号”刷新受影响编号。

### 3.4 统一生成全部编号

完成所有节点名称后，点底部「**统一生成全部编号**」。

- 若有**同级同名**（同一上级、同一类型出现重复名称），会先弹确认框：
  - 提示“检测到重复名称：……。确定仍要生成吗？”
  - 「仍然生成」→ 继续生成（重复名称仍允许，编号按序号区分）；「返回修改」→ 返回。
- 生成成功后，节点状态由「草稿」变为「已生成」，并显示各自编号。

### 3.5 生成后的操作

- **领取**：点「领取」，系统记录领取人并**复制编号**到剪贴板。
- **修改**：点「修改」，可改名称与编号，保存时系统校验编号格式与唯一性。
- **删除**：删除节点及其下级与领取记录。
- **导出 Excel**：右上角「导出 Excel 表」，导出该项目产品组成编码表。
- **增加整机**（仅整机类型）：顶部「增加整机」可加多台整机。

### 3.6 层级编号格式

编号分段随层级递进，末尾为「阶段－版本」。示例（阶段 `C` 为初样/电性件，`Z` 为正样件）：

| 层级 | 编号格式（示例） |
|---|---|
| 整机 | `GH{项目号}-{缩写}-00-C-1.00` |
| 部组件（根节，结构/板卡类型） | `GH{项目号}-01-00-C-1.00` |
| 整机下部组件 | `GH{项目号}-{缩写}-01-00-C-1.00` |
| 结构/硬件/其他 | `GH{项目号}-{缩写}-01-01-C-1.00` |
| 零件 | `GH{项目号}-{缩写}-01-01-01-C-1.00` |

- 阶段 `Z` → 版本 `2.00`；其余阶段 → 版本 `1.00`。
- 同一产品的不同系列（如 时序板Ⅰ/Ⅱ/Ⅲ、配电器A/B）会用**不同功能码**（示例：`SX1`、`SX2`、`SX3`；`PD1`、`PD2`），同一系列内部保持一致。

---

## 四、角色与权限

- **普通用户**：只能进入「用户界面」，做上述查询、领取、申请、编制操作。
- **管理员**：有独立的「管理员界面」，负责建立/初始化项目、审核异常与相似名称、特殊编号项目人工编号、管理编码库等。
- 普通用户提交的异常名称、相似名称、新项目申请、特殊编号申请，都会进入管理员界面由管理员处理，处理结果在用户的「我的申请」等位置反馈。

---

## 五、常见问题速查

| 现象 | 说明 / 建议 |
|---|---|
| 输入项目号后产品名称没有弹出可选项 | 该项目**未登记**这些产品/板卡/软件名。需要管理员在项目初始化时登记；也可不选、直接手工输入。 |
| 点“查询编码”没反应、提示“请先选择项目” | 还没选中项目。输入到完整 4 位项目号会自动选中，或手动点击列表项。 |
| 提示“文件名称校验通过，编号已生成” | 名称正常，已自动编号，可直接领取。 |
| 提示“检测到相似文件名称，已提交管理员审核” | 名称与库中某文件相似，转管理员确认；在「我的申请」看结果。 |
| 提示“该项目有其他编号要求，请等待管理员人工编号” | 该项目是“特殊编号项目”，新编号需管理员人工编。 |
| 产品组成里出现“同级同名”弹窗 | 同一上级下填了重名。可“仍然生成”（编号按序号区分）或“返回修改”。 |
| 软件/逻辑无法在产品组成里编号 | 页面提示：软件/逻辑请到「文件编号」处编号，以保持与项目文件编号规则一致。 |

---

*本教程基于当前系统实现整理，如需最新的编号规则细节，请以管理员配置的《文件简号.xlsx》与编号规则为准。*
